# ---- Build output ----
target/
build/
out/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ---- IDE ----
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# ---- OS / editor ----
.DS_Store
Thumbs.db
*~
*.swp

# ---- Logs / runtime ----
*.log
/tmp/
logs/

# ---- Runtime databases (file-based H2 for the OPC order store; created on first run) ----
data/
*.mv.db
*.trace.db

# ---- PowerPoint lock files ----
~$*.pptx

# ---- Legacy input source (the app we migrated FROM — reference only, not part
#      of the migrated deliverable). Kept locally, excluded from the repo. ----
petstore1.3.1_02/
petstore-1_3_1_02.zip

# ---- Python (diagram/ppt generators) ----
__pycache__/
*.pyc
.venv/
venv/

# ---- Secrets: never commit private keys (this is a PUBLIC repo) ----
# The RSA PRIVATE key is generated locally by ./generate-keys.sh and must NOT be
# committed. The PUBLIC key (petstore-auth-public.pem) is meant to be shared and
# IS committed so verifier services work out of the box.
auth-service/keys/private_key*.pem
auth-service/app/resources/auth-private-key.pem
